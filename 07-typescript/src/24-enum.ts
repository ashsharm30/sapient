// enum represents a set of allowed values

// defining an enum
enum FillColor {
    RED = 'red',
    GREEN = 'green',
    BLUE = 'blue'
}

let color : FillColor;

color = FillColor.BLUE; // using an enum

console.log( color );