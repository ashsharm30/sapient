const moviePrototype = {
    addToCollection( amount ) {
        this.collection += amount;
    }
};

/**
 * Define animationMoviePrototype
 */
// your code ...

/**
 * Define some objects representing animated movies
 * - name, collection, animationStudio, resolution = '4K'
 * - changeResolution( resolution ) : Sets new resolution for the object
 */
// your code ...