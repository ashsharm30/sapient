// Generally we have boolean values on either side of the || operator
// var x = x < y || y < z;

// In JS we can have any value (of any data type) on either side of the || operator
var x = 1 || 'hello';